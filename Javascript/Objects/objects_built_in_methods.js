//objects keys
//return an array of objects

const user = {
    name: "Devraj",
    age : 18
}

console.log(Object.keys(user))

//returns array of keys

object.values(user)
//returns arrays of value
object.entries(user);
