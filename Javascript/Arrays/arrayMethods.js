// sort  sorting arrays... 

let fruits = ["banana", "apple", "cherry"];
fruits.sort();
console.log(fruits); // ["apple", "banana", "cherry"]

// numbers can't be sorted properly due to js. converting 
//numbers into string.

